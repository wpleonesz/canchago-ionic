# Modo desarrollo (yarn dev): ruta relativa — Vite la proxya a la API real (ver vite.config.ts).
# Así el navegador ve todo como mismo origen y la cookie HttpOnly de sesión funciona sin CORS.
VITE_API_BASE_URL=/api
VITE_API_TIMEOUT_MS=15000
